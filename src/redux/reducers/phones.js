initState = {
    data: null,
    status: 'INITIAL'
};

const phones = (state = initState, action) => {
    switch (action.type) {
        default:
            return state;        
    }
};

export default phones;