initState = {
    data: null,
    status: 'INITIAL'
};

const comments = (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    };
};

export default comments;