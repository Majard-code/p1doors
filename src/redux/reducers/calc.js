initState = {
    data: null,
    dataChoice: null,
    status: 'INITIAL'
};

const calc = (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    };
};

export default calc;