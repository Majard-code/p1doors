initState = {
    data: null,
    status: 'INITIAL'
};

const doors = (state = initState, action) => {
    switch (action.type){
        default:
            return state;
    };
};

export default doors;